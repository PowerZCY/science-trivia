import type { SiteNavItemConfig } from '@windrun-huaiin/third-ui/fuma/base/site-layout-shared';

export async function homeHeavyItems(_locale: string): Promise<SiteNavItemConfig[]> {
  return [
    
  ];
}
